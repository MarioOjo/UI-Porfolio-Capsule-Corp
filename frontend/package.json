{
  "name": "capsule-corp-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs');const p='node_modules/.cache';if(fs.existsSync(p)){try{fs.rmSync(p,{ recursive:true, force:true });console.log('Removed',p);}catch(e){console.warn('Could not remove',p,e.message);} }\"",
    "dev": "vite",
    "prebuild": "node -e \"const fs=require('fs');const p='node_modules/.cache';if(fs.existsSync(p)){try{fs.rmSync(p,{ recursive:true, force:true });console.log('Removed',p);}catch(e){console.warn('Could not remove',p,e.message);} }\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^4.0.0",
    "express": "^4.18.2"
  }
}
